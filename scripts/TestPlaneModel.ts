// import TestManagementRestClient from "TFS/TestManagement/RestClient";
import { TestPlan } from "TFS/TestManagement/Contracts"; 


export class TestPlaneModels {

    constructor (projectName: string)
    {
        // let client = TestManagementRestClient.getClient();
        // client.getPlans(projectName).then((plansList:TestPlan[])=>{
              
        // })  
    }
}